# mini-cli

Fixture de los evals del plugin `chichex-skills`: un CLI que suma números (`node src/cli.mjs 1 2 3` → `Total: 6`).
Sirve de proyecto de prueba para el caso `adherence-sdd-spec`; no es un paquete real.
